machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install pyyaml requests

test:
  pre:
    - git clone https://rainierwolfcastle:$GITHUB_PAT@github.com/ARMmbed/testbench-client.git
  override:
    - python testbench-client/testbench-client.py testbench.yml
