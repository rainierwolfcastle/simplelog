machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install pyyaml

test:
  pre:
    - git clone https://rainierwolfcastle:$GITHUB_PAT@/github.com/rainierwolfcastle/simplelog.git
  override:
    - nosetests
